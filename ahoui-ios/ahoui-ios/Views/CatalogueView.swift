import SwiftUI

struct CatalogueView: View {

    var body: some View {
        NavigationView {
                VStack(spacing: 16) {
                    Text("CATALOGUE VIEWWWW")
                        .font(.system(size: 25, weight: .semibold))
                        .padding(.top, 20)
                }
                .padding()
            .background(Color(red: 1, green: 0.965, blue: 0.922))
        }
    }
}
